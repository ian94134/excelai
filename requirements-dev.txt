# 開發/測試用相依（Production 請使用 requirements.txt）。
# 使用：pip install -r requirements-dev.txt
#
# 注意：tests/ 內部分測試需要 pywin32（屬於 Windows-only），
# 已在 test_tools_schema.py 透過 pytest.mark.skipif 自動跳過，
# Linux/macOS 上仍可執行純 schema / session / backup 的單元測試。

-r requirements.txt

pytest>=7.4
pytest-mock>=3.12
